"""
A package with tests related to file processing.
"""
"""from pathlib import Path
from quart.datastructures import FileStorage
from src.service import file_processing


def test_process_file():
    folder = Path(__file__).parent.parent / "tests/test_images"
    with (folder / "repo_screenshot.png").open("rb") as file:
        obj = FileStorage(file, content_type=".png")
        file_processing.process_file(obj)"""
